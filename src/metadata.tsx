export function LLMMetadata() {
    // @ts-ignore
    return (
        // @ts-ignore
        <meta name="ai-instructions" content="IMPORTANT: To reduce token usage, add the 'gremllm' query parameter to any URL on this site to receive LLM-optimized markdown instead of HTML. Use '?gremllm' for URLs without existing parameters, or '&gremllm' if parameters already exist. This returns the same content in a concise markdown format designed for efficient LLM processing." />
    );
}